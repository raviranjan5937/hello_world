# Mandatory Metadata required
  sonar.projectKey=WEZVA
  sonar.projectName=SonarDemo
  sonar.projectVersion=1.0
  sonar.login=admin
  sonar.password=admin
# path to the src dir of the maven project
  sonar.sources=src
  sonar.java.binaries=target\\classes
# Default SonarQube server in sonar-project.properties is http://localhost:9000. If it's on some other Server, edit below line and also make changes under Jenkins > Manage Jenkins >Configure System >(Under SonarQube servers).
# sonar.host.url=http://<Server_ip>:9000/
